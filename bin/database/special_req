#!/bin/sh

psql -c "
    SELECT
        registration.id,
        person.lastname || ', ' || person.firstname,
        special
    FROM
        registration,
        person
WHERE
    registration.person_id = person.id
    AND
    special != ''
 ORDER BY
    person.lastname,
    person.firstname
"


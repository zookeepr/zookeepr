#!/bin/sh

psql -U zookeepr -c "
    SELECT
        registration.id,
        person.lastname || ', ' || person.firstname,
        diet
    FROM
        registration,
        person
WHERE
    registration.person_id = person.id
    AND
    diet != ''
 ORDER BY
    person.lastname,
    person.firstname
"


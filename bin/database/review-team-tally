#!/usr/bin/env bash

psql -c '
    SELECT
        person.firstname,
        person.lastname,
        count(review.reviewer_id)
    FROM
        review,
        person
    WHERE
        person.id = reviewer_id
    GROUP BY
        firstname,
        lastname,
        reviewer_id
    ORDER BY
        count DESC
'

#echo Add the numbers below together

echo extra tickets
psql -c "select SUM(dinner) from registration where id in (SELECT rego from rego_invoice_payment where payment > 0);"

echo professional
psql -c "select COUNT(id) from registration where id in (SELECT rego from rego_invoice_payment where payment > 0) and type='Professional';"

#echo discount_code
#psql -c " select COUNT(id) from registration where registration.discount_code = discount_code.code;"

#echo speakers
#psql -c "select count(*) from speakers;"

echo extra volunteers 10
#echo okl 10
